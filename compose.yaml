services:
    php:
        build: ./dev/docker
        environment:
            HOST_USER_ID: ${HOST_USER_ID}
            HOST_GROUP_ID: ${HOST_GROUP_ID}
            HOST_IP: ${HOST_IP:-}
            SYMFONY_REQUIRE: ${SYMFONY_REQUIRE:-6.4.*}
        volumes:
            - .:/app/src
